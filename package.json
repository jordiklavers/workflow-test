{
  "name": "workflow-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --minify --outfile=dist/app.js --loader:.css=css && cp loader/loader.js dist/loader.js",
    "watch": "esbuild src/index.js --bundle --outfile=dist/app.js --loader:.css=css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.27.2"
  }
}
